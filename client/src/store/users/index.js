const initialState = {
  isLoggedIn: false,
  username:   '',
  role:       'USER'
};

const users = (state = initialState, action) => {
  switch (action.type) {

  default:
    return state;
  }
};

export default users;
