import React from 'react';
import Typography from '@material-ui/core/Typography';

const Publish = () => {
  return (
    <Typography variant="h4">Publish Result</Typography>
  );
};

export default Publish;
