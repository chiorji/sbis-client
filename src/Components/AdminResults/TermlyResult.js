import React from 'react';
import Typography from '@material-ui/core/Typography';

const TermlyResult = () => {
  return (
    <Typography variant="h6">Termly Result</Typography>
  );
};

export default TermlyResult;
